/*
import * as actionTypes from '../actions';

export const changeSetting = (setting, value) => dispatch => {
    dispatch({
        type: actionTypes.CHANGE_DEFAULT_SETTING,
        setting,
        value
    });
};*/
